# Environment Variables Template
# Variables in {{VARIABLE}} format will be substituted with GitHub context values

# Version information
VERSION={{GIT_TAG}}
BUILD_SHA={{GIT_SHA}}
BUILD_DATE={{BUILD_DATE}}

# Environment context
ENVIRONMENT={{ENVIRONMENT}}
BRANCH={{BRANCH}}

# Repository information
REPOSITORY={{REPOSITORY}}
COMMIT_MESSAGE={{COMMIT_MESSAGE}}

# CI/CD metadata
CI=true
GITHUB_ACTIONS=true
WORKFLOW={{WORKFLOW}}
RUN_ID={{RUN_ID}}
RUN_NUMBER={{RUN_NUMBER}}